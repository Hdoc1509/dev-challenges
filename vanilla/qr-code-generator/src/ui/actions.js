import { getElementById } from "@lib/dom";

export const $downloadQRImage = getElementById(
  "download-qr-image",
  HTMLButtonElement,
);
export const $shareQRQuote = getElementById(
  "share-qr-quote",
  HTMLButtonElement,
);
