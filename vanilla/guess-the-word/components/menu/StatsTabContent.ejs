<% for (const category of STATS_CATEGORIES) { %>
<% if (category === DIFFICULTY.WHY) { %>
<%- include('Progress', { category, onTitle: title => `${title}?` }) %>
<% } else { %>
<%- include('Progress', { category }) %>
<% } %>
<% } %>
