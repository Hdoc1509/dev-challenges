export const CLASSES = Object.freeze({
  TIMER_BAR: "alert__time-bar",
  TEXT: "alert__text",
});

export const CSS_VARIABLES = Object.freeze({
  TIME_BAR_DURATION: "--alert-time-bar-duration",
});

export const COLORS = Object.freeze({
  SUCCESS: "success",
  ERROR: "error",
});
