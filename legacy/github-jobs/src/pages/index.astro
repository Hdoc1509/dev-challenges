---
import App from "@/App";
import Maintenance from "@/components/Maintenance.astro";
import Layout from "../layouts/Layout.astro";
import { isDev } from "@/config";
---

<Layout>
  {isDev ? <App client:only="react" /> : <Maintenance issue={15} />}
  <!-- <App client:only="react" /> -->
  <!-- <Maintenance /> -->
</Layout>
