---
to: vanilla/<%= name %>/package.json
---

{
  "name": "<%= name %>",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@lib/dom": "workspace:",
  },
  "devDependencies": {
    "vite": "catalog:",
    "vite-plugin-html": "catalog:"
  }
}
