---
to: ssr/<%= name %>/.prettierrc.js
---

export default {
  plugins: ["prettier-plugin-astro"],
  overrides: [
    {
      files: ["*.astro"],
      options: {
        parser: "astro",
      },
    },
  ],
}
